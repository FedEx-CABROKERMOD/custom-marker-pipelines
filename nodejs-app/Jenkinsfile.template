pipeline {
    agent { label 'nodejs-app' }
  options { 
    buildDiscarder(logRotator(numToKeepStr: '3'))
  }
  stages {
   stage('Test') {
      steps {
        container('nodejs') {
          echo 'Hello World!'   
            sh 'node --version'
        }
      }
    }
  }
}
